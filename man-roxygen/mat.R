#' @param mat [\code{matrix}]\cr object to which the spatial pattern is added.
#' @details If this function is used in conjunction with \code{\link{generate}},
#'   \code{mat} cannot be specified, because \code{generate} assigns it.
